# Local Setup

1. `source env.sh`to export variable needed to complete the process
2. `sh install_kfctl.sh`to install kfctl if you haven't
3. `sh setup.sh`to install kubeflow on your local minikube cluster

## Prerequirements

 - Minikube
 - kubectl
 - Docker 
 - Python
